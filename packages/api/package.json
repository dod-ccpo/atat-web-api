{
  "name": "atat-web-api",
  "version": "0.1.0",
  "description": "The ATAT Web API is the internal API that supports the ATAT Web UI.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint --ext .js,.ts,.json -c ../../.eslintrc.json .",
    "lint:fix": "npm run lint -- --fix"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.25.0",
    "@aws-sdk/lib-dynamodb": "^3.25.0",
    "@aws-sdk/util-dynamodb": "^3.25.0",
    "@types/aws-lambda": "^8.10.81",
    "eslint": "^7.31.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/uuid": "^8.3.1",
    "eslint": "^7.31.0"
  }
}
