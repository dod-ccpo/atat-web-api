import { handler } from "./generate-document";

describe("Success", () => {
  it("should return successful", async () => {
    // GIVEN / ARRANGE
    // WHEN / ACT
    // THEN / ASSERT
  });
});

describe("Failure", () => {
  it("should return errors", async () => {
    // GIVEN / ARRANGE
    // WHEN / ACT
    // THEN / ASSERT
  });
});
